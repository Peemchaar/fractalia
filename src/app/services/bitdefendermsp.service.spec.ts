/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { BitdefendermspService } from './bitdefendermsp.service';

describe('Service: Bitdefendermsp', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [BitdefendermspService]
    });
  });

  it('should ...', inject([BitdefendermspService], (service: BitdefendermspService) => {
    expect(service).toBeTruthy();
  }));
});
